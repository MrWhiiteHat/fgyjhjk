# Root environment template for local orchestration
# Backend service variables live in app/backend/.env.example
# Frontend service variables live in app/frontend/.env.local.example

# Frontend -> backend API URL used by docker-compose frontend service
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Optional override for docker compose project name
COMPOSE_PROJECT_NAME=realfake
