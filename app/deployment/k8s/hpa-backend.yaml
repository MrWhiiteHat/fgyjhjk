apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: realfake-backend-hpa
  namespace: realfake
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: realfake-backend
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
